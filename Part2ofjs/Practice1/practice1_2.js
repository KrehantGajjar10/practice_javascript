 /*
 
 // Text/content access: innerText, textContent, innerHTML

 let h1 = document.querySelector("h1"); 

 h1.innerText = "Hello World";            // innerText/ textContent will change the text of the element.
 h1.textContent = "Welcome to JavaScript";
 h1.innerHTML = "<i>Welcome to JavaScript</i>";   // But innerHTML will change the html content of the element.
  // h1.hidden = true;                         // hidden is a property which will hide the element when its value is true. It is not an attribute, so we cannot use setAttribute to set its value. We have to set its value directly.

  */